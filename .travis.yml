# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language:
  - r

before_install:
  - curl https://sh.rustup.rs -sSf | sh --default-toolchain stable -y

cache: packages
latex: false
fortran: false
pandoc: false

cache: packages
