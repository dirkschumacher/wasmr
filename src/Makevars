LIBDIR = myrustlib/target/release
STATLIB = $(LIBDIR)/libwasmer_runtime_c_api.a
PKG_LIBS = -L$(LIBDIR) -lwasmer_runtime_c_api -lresolv

#all: clean

$(SHLIB): $(STATLIB)

$(STATLIB):
	cargo build --release -p wasmer-runtime-c-api --manifest-path=myrustlib/Cargo.toml

#clean:
#	rm -Rf $(SHLIB) $(STATLIB) $(OBJECTS) myrustlib/target
